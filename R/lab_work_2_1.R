C <- 0.158
DeltaC <- 0.0005

M <- 1.6531
DeltaM <- 0.0003

N <- 3.78
DeltaN <- 0.002

FunLabWork_2_1(DeltaC = DeltaC,
               DeltaM = DeltaM,
               DeltaN = DeltaN,
               C = C,
               M = M,
               N = N)

cat("=============== \n")

C <- 0.235
DeltaC <- 0.0003

M <- 2.348
DeltaM <- 0.002

N <- 4.37
DeltaN <- 0.004

FunLabWork_2_1(DeltaC = DeltaC,
               DeltaM = DeltaM,
               DeltaN = DeltaN,
               C = C,
               M = M,
               N = N)
